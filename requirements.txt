# =============================================================
# Desktop Character Pet —— 依赖清单
# 安装必需依赖：pip install -r requirements.txt
# 可选依赖缺失时程序自动降级，不影响核心对话功能。
# =============================================================

# ---------- 必需依赖 ----------
PySide6>=6.5.0        # GUI 框架
requests>=2.31.0      # LLM API / GPT-SoVITS 调用

# ---------- 可选依赖（按需安装）----------
# L1 长期记忆（ChromaDB 向量检索）+ 角色漂移检测（嵌入模型）
#   chromadb 负责向量库存储；sentence-transformers 负责文本嵌入。
#   不安装时：L1 记忆、锚点、OOC 检测静默降级。
#   首次启动程序时会检测缺失并询问是否自动安装（走国内镜像源）。
chromadb>=0.4.22
sentence-transformers>=2.2.0

# TTS 语音输出（GPT-SoVITS）
#   无需额外 Python 包：需自行部署 GPT-SoVITS 推理服务（api.py，默认端口 9880），
#   并在角色设置中指定"语音包文件夹"路径。服务不可达时自动降级为纯文本。
